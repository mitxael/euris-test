@model EURISTest.Models.ProdottoModel

@{
    ViewBag.Title = Model.Id;
}

<h2>Prodotto #@Model.Id</h2>

@using (Html.BeginForm("Save", "Prodotto"))
{
    @Html.ValidationSummary(true, "Please fix the following errors.")
    <div class="form-group">
        @Html.LabelFor(m => m.Codice)
        @Html.TextBoxFor(m => m.Codice, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Codice)
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.Descrizione)
        @Html.TextBoxFor(m => m.Descrizione, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Descrizione)
    </div>
    @Html.HiddenFor(m => m.Id)
    @Html.AntiForgeryToken()
    <button type="submit" class="btn btn-primary">Save</button>
}